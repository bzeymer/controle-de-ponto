/* import {  } from '../actions/playlist'; */

export default function (state = [], action) {
    switch (action.type) {
        /* case FETCH_VISIBILIDADES:
            return action.payload; */
        default:
            return state;
    }
}
